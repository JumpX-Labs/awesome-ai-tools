# JumpX · README 驱动工具展示站 PRD（课件修订版）

## 以落地结果为重写的导读（供课件使用）

本节用「需求 → 已交付」视角总结当前仓库状态，便于课堂对照；**后文保留原始 PRD 论述**，可用来讲「规划、取舍、与最终实现为何不同」。

### 命名与定位

- **站点标题**：根目录 `README.md` 一级标题为 **「JumpX AI 产品与工具推荐」**，面向学员与读者。
- **产品形态**：不是传统网址导航后台，而是 **README 驱动的工具索引站**：维护者只改 Markdown，访问者用搜索与分类快速跳转外链。

### 信息架构（已实现）

| 能力 | 实现要点 |
|------|----------|
| 数据源 | 仅 `README.md`；`##` 为分类，`- [名](URL) - 描述` 为条目（见 `CONTRIBUTING.md`）。 |
| 构建 | `npm run parse` / `prebuild` / `predev` → 生成 `public/tools.json`（`.gitignore` 忽略，CI 产物思路）。 |
| 「全部」 | 前端合成 tab；与当前分类、搜索关键词为 **AND** 过滤。 |
| ALL 视图 | 按 README 中分类顺序分节展示卡片，避免「一锅粥」。 |
| 搜索 | 名称、描述、URL、分类；**拼音匹配**（`pinyin-match`）；`/` 聚焦；**Enter** 打开当前列表首条（在搜索框聚焦或已有搜索词时）。 |
| 主题 | 浅 / 深 / 跟随系统；`localStorage` 键 `jumpx-theme`。 |

### 技术栈（已实现）

- **前端**：React 19 + Vite 6 + TypeScript；纯静态 `fetch(tools.json)`。
- **解析**：Node 脚本 `scripts/parse-readme.mjs`，`--validate` 严格失败即非零退出。
- **CI**：`.github/workflows/validate-readme.yml` 在 PR/Push 上跑 `npm run validate`。

### 视觉与品牌（已实现）

- **JumpX**：顶栏标识、浏览器标题后缀、CSS 注释头。
- **风格**：**Editorial Dev Tool + Brutalist** — 浅灰/冷白与纯黑结构色、`#ccff00` 荧光绿强调、**零圆角**、**2px 黑边**、**硬阴影**（如 `4px 4px 0 #000`）；**Instrument Sans + IBM Plex Mono**。

### 部署与发布（组织标准 vs 原文 PRD）

- 原文 PRD 以 **GitHub Pages** 为优先表述；**JumpX 实战营课件发布** 推荐按组织 Skill 使用 **GitHub（如 `JumpX-Labs/*`）+ Cloudflare Pages + GitHub Actions（`wrangler-action@v3`）**；本仓库生产访问域名为 **`https://tools.jumpxai.com`**（在 Cloudflare Pages 项目 `awesome-ai-tools` → Custom domains 中绑定）。Secrets 使用 **Cloudflare Dashboard 创建的 API Token**（勿用 `wrangler login` 的 OAuth Token）。
- 本仓库可按同一 Skill 完成「建库—推代码—配 Pages—绑域名—注 Secrets」；`jumpxai.com` 托管在 Cloudflare 时，绑定 `tools.jumpxai.com` 后 DNS 通常由系统自动下发，无需在 DNS 面板手写 CNAME（以 Dashboard 提示为准）。

### 与后文原始章节的差异（可课堂讨论）

| 主题 | 原始 PRD 倾向 | 当前落地 |
|------|----------------|----------|
| 前端路线 | 原生 MVP → 再 Vite | **直接 Vite + React**。 |
| 视觉 | 圆角、克制阴影 | **Brutalist 直角 + 硬阴影 + 高对比**。 |
| 部署 | GitHub Pages 优先 | **Cloudflare Pages + Actions** 为组织推荐主路径。 |

---

## 原始 PRD 正文

以下条件自本节起为立项时的需求叙述，保留以便对照上文「导读」中的落地结果。

> **说明**：下文仍出现「优先 GitHub Pages」「原生静态 vs Vite」等立项期表述时，以文档开头「导读」中的**当前实现与推荐部署路径**为准。

## 项目概述

本项目的目标是建设一个“README 驱动”的个人工具展示站：以 GitHub 仓库中的 `README.md` 作为唯一核心数据源，通过静态构建生成一个具有设计感的前端展示页面，用于展示个人常用的软件、AI 工具、开发工具和工作流工具。

项目参考两个现有仓库：`YSGStudyHards/Awesome-Tools` 作为内容组织与数据表达的参考，其核心价值在于使用 GitHub README 维护工具目录；`van579/nav` 作为交互和前端体验参考，其核心价值在于简洁的搜索优先导航体验，但其整体工程结构明显包含 Go 后端、数据库、API、后台管理和 Docker 部署，并不适合作为本项目的整体技术基础。

本项目不是传统“网址导航站”，也不是自托管服务仪表盘，而是一个面向学员、读者或公开访问者的“个人工具栈展示站”。页面需要具备卡片墙、分类浏览、搜索过滤、快速跳转、暗色模式和良好的移动端适配，同时保持纯静态、易部署、易维护的特性。

## 背景与动机

传统 Awesome List 的优势在于维护简单、开源友好、天然适配 GitHub 协作流程，但其展示体验通常停留在 Markdown 阅读层，难以体现个人品牌和产品化设计感。

另一方面，导航站类项目如 `Homepage`、`发现导航` 和 `van-nav` 在卡片布局、搜索入口、主题切换和导航效率方面已经提供了成熟思路，但多数项目带有更复杂的数据源、后台配置或服务管理假设，超出本项目的需求边界。

因此，最合适的中间方案是：保留 Awesome List 的 GitHub 可维护性，将内容维护工作全部收敛到 `README.md`；同时通过一个独立静态页面，将这些 Markdown 数据解析为结构化卡片与分类导航，提供明显优于 README 原生阅读体验的视觉和交互层。

## 项目目标

### 业务目标

- 构建一个可以长期维护的个人工具展示仓库。
- 让普通访问者无需阅读长篇 README，也能快速理解“正在使用哪些工具、它们分别做什么、为何推荐”。
- 面向学员或读者提供一个可以直接分享的入口页，用于展示常用 AI 工具和生产力工具。
- 保持完全静态部署能力，优先支持 GitHub Pages，必要时可迁移到 Cloudflare Pages 等静态托管平台。

### 产品目标

- 实现 `README.md` 到页面展示的单向内容流。
- 支持分类浏览、关键词搜索、卡片点击跳转、主题切换、移动端适配。
- 保证内容编辑者只需要修改 README，而不需要手改前端页面。
- 页面在视觉上具备“设计感”，但避免传统导航站的堆砌感和后台仪表盘风格。

### 技术目标

- 无后端、无数据库、无账号系统、无管理后台。
- 构建输出为纯静态页面。
- 解析逻辑应稳定、可维护，并尽量容忍 README 的日常小幅变动。
- 后续可支持从 README 预构建成 JSON，以提升渲染稳定性和搜索性能。

## 非目标

以下内容明确不纳入本期范围：

- 不实现后台管理系统。
- 不引入数据库和服务端 API。
- 不支持登录、权限、个人收藏、统计报表等 SaaS 化能力。
- 不以自托管服务面板为目标，不展示服务器状态、小组件、监控数据等 dashboard 能力。
- 不追求复杂的拖拽布局编辑能力；“调整”仅指通过 README 数据顺序或简单前端排序实现，而不是可视化后台拖拽。

## 用户与使用场景

### 核心用户

| 用户角色 | 需求 | 主要行为 |
|---|---|---|
| 仓库维护者 | 用尽量低成本维护工具清单 | 修改 README、提交 Git、预览页面 |
| 学员/读者 | 快速查看推荐工具及用途 | 搜索工具、按分类浏览、点击官网 |
| 技术同行 | 了解工具栈与工作流 | 浏览卡片、查看描述、跳转外部链接 |

### 核心场景

1. 访问者进入首页，立即看到一段简短介绍以及最常用的工具分类。
2. 访问者通过搜索框输入 “Claude” 或 “终端” 直接筛选结果。
3. 访问者点击某个工具卡片，跳转到对应官网或产品页。
4. 维护者新增一个工具，仅需在 README 对应分类下新增一行。
5. 构建脚本重新生成结构化数据，静态页面自动展示新工具。

## 参考项目分析

### 参考一：Awesome-Tools

`YSGStudyHards/Awesome-Tools` 的价值不在于视觉层，而在于“以 README 作为主要内容容器”的组织方式。该模式天然适合 GitHub 阅读、历史版本管理、PR 协作和开源传播，也与 Awesome List 的常见维护方式一致。

本项目应借鉴其内容层设计原则：分类清晰、条目简洁、链接明确、描述信息尽量压缩到一行，使 Markdown 既可直接阅读，又能被程序稳定解析。

### 参考二：van579/nav

`van579/nav` 的亮点在于简洁直接的前端交互：搜索优先、卡片式浏览、暗色模式、移动端适配以及面向快速访问的导航体验。

但该项目整体并不适合作为本项目的架构基础，因为其工程包含明显的后端入口、数据库文件、API 文档、后台管理和 Docker 运行方式，这说明它服务的是“可管理导航站系统”，而不是“纯静态 README 展示页”。因此，本项目仅应借鉴其前端交互理念，而不应继承其整体技术结构。

### 参考三：Homepage

`Homepage` 之所以值得参考，是因为它已经证明：卡片墙、搜索入口、深色主题和高可定制首页完全可以呈现出成熟的产品感。它更像产品化 dashboard，但在视觉层级、卡片组织与主题统一性方面，对本项目具有直接参考价值。

本项目不应复刻其自托管服务面板属性，但可以吸收其版式节奏、卡片密度控制、搜索入口优先级和深色模式完成度。

## 产品定位

本项目定位为：

> 一个面向公开访问者的、由 README 驱动的、具备产品级设计感的个人工具展示站。

它兼具三种属性：

- GitHub Awesome List：维护简单，适合公开仓库传播。
- 导航卡片站：便于浏览和点击跳转。
- 轻量品牌展示页：能够体现个人方法论，而不只是链接堆积。

## 信息架构

### 数据源层

唯一主数据源为仓库根目录下的 `README.md`。README 中按分类组织工具条目，每个条目至少包含：工具名称、外部链接、简短说明。

推荐的最小 Markdown 结构如下：

```md
# Winko's Tools

## AI 工具
- [Claude](https://claude.ai) - 长文本分析与写作
- [ChatGPT](https://chatgpt.com) - 通用问答与代码辅助
- [Warp](https://www.warp.dev) - AI 终端与命令行工作流

## 开发工具
- [GitHub](https://github.com) - 代码托管与协作
```

### 结构化数据层

构建阶段将 README 解析为 JSON，输出标准结构，例如：

```json
[
  {
    "category": "AI 工具",
    "items": [
      {
        "name": "Claude",
        "url": "https://claude.ai",
        "description": "长文本分析与写作"
      }
    ]
  }
]
```

此设计借鉴了 Markdown 转 JSON 的成熟思路，可降低前端渲染复杂度，并避免直接解析 HTML 结构带来的脆弱性。

### 展示层

前端页面从 JSON 读取数据后，渲染为以下结构：

- 顶部 Hero 区：站点标题、简短介绍、搜索框。
- 分类导航区：展示所有分类，可点击筛选。
- 卡片墙区：展示当前分类或搜索结果下的工具卡片。
- 页脚区：仓库链接、更新时间、README 驱动说明。

## 功能需求

### F1. Markdown 数据解析

系统需要能够读取仓库中的 `README.md`，并将符合约定格式的分类和条目转换为结构化数据。

功能要求：
- 支持识别二级标题 `##` 作为分类。
- 支持识别 `- [名称](链接) - 描述` 作为工具条目。
- 对不符合格式的内容进行跳过或容错处理。
- 构建失败时提供明确的错误提示，指出哪一段 Markdown 不符合预期。

### F2. 分类浏览

页面需展示所有分类，并允许用户点击分类后过滤卡片墙内容。分类按钮需支持高亮当前状态，移动端应支持横向滚动或折叠菜单。

### F3. 搜索过滤

页面应提供常驻搜索框，支持按工具名称和描述进行前端模糊搜索。搜索结果需实时反馈，并与分类过滤协同工作。

推荐支持的交互包括：
- 页面加载后按快捷键立即聚焦搜索框。
- Enter 打开当前首个匹配项。
- 清空搜索后恢复当前分类结果。

### F4. 卡片展示

每个工具条目应渲染为独立卡片，至少展示：
- 工具名称
- 一句简短描述
- 所属分类（可选）
- 外部链接入口

卡片点击区域应尽量大，以提升移动端和桌面端的命中效率。整体布局需兼顾视觉节奏与扫描效率，避免传统网址导航站的密集堆叠。

### F5. 主题与视觉

系统需支持浅色/深色主题，默认遵循系统主题，同时允许手动切换。页面应使用更接近现代产品站的视觉层级，而非传统导航站风格。

设计原则：
- 颜色克制，以中性背景和单一强调色为主。
- 卡片层级清晰，避免多余装饰。
- 搜索框和标题区具备明显品牌感。
- 卡片 hover、聚焦和点击反馈明确。

### F6. 响应式支持

页面需要在桌面端和移动端都具备良好体验。桌面端采用多列卡片布局，移动端自动降为单列或双列，并保证搜索框、分类栏和卡片点击区的可操作性。

### F7. 部署与分享

系统需支持通过 GitHub Pages 部署为静态站点，也应兼容其他静态托管平台。访问者应能直接通过公开 URL 访问页面，无需后端支持。

## 可选增强需求

以下功能不属于首期必须项，但在架构上应保留扩展空间：

- 标签系统：一个工具可同时具备多个标签。
- 推荐等级或“正在使用”标识。
- 按最近更新排序。
- 多语言支持（中英双语字段）。
- 自动抓取站点 favicon 或品牌图标。
- 从 README 升级为 `README + data/tools.json` 双源兼容。
- 生成单个工具详情弹层或详情页。

## 交互设计原则

### 搜索优先

搜索框应是页面的核心控件，而不是附属元素。对于已知工具名的访问者，最快路径是搜索直达，而不是逐层展开分类。

### 分类辅助

分类用于降低初次浏览成本，但不应成为唯一入口。用户既可以先选分类再浏览，也可以直接搜索，再由结果反向理解分类结构。

### 卡片克制

卡片应强调内容密度与可读性，避免使用大量图标圆底、夸张阴影或“模板化 SaaS 首页”元素。页面要有设计感，但不应滑向装饰性过强的视觉套路。

### Git 工作流替代后台

所有“编辑”行为都应通过修改 README 完成，而不是增加管理页面。这既符合开源协作方式，也使系统保持纯静态和低维护成本。

## 技术方案

### 总体架构

推荐采用以下架构：

1. 仓库维护 `README.md`。
2. 使用 Node.js 脚本在构建时解析 README。
3. 输出 `tools.json`。
4. 静态前端页面读取 `tools.json` 并渲染 UI。
5. 使用 GitHub Pages 托管构建结果。

### 前端实现建议

推荐采用以下两种路径之一：

| 路线 | 说明 | 适用阶段 |
|---|---|---|
| 原生静态 HTML/CSS/JS | 最轻量，适合快速验证视觉和交互 | MVP |
| Vite + 轻量前端框架 | 更适合后续组件化和扩展 | 第二阶段 |

如果首期目标是快速验证 README 驱动是否顺手，优先建议使用原生静态方式；若后续需要动画、组件复用和更强的状态管理，再升级到 Vite 体系。

### Markdown 解析建议

如果是运行时直接读取 Markdown，可用 `marked` 或 `markdown-it` 进行解析；如果是构建时抽取结构化数据，则优先使用 Markdown AST 或自定义正则解析，避免依赖最终 HTML 结构。

对本项目而言，更推荐“构建时解析为 JSON”，理由如下：
- 数据结构更稳定。
- 搜索和过滤性能更好。
- 前端更容易做卡片化定制。
- 可逐步扩展字段，不受 Markdown HTML 输出形式限制。

## 推荐的 README 规范

为了让解析稳定，README 应遵循如下约束：

- 一级标题仅用于站点标题。
- 二级标题作为一级分类。
- 每个条目一行，不跨行。
- 条目格式固定为：`- [名称](URL) - 描述`
- 避免在分类区混入复杂嵌套列表、引用块、表格和图片。
- 如需额外信息，可在后续扩展为自定义语法，例如：
  `- [Claude](https://claude.ai) - 长文本分析 #AI #writing`

## 页面结构建议

### 首页模块

1. 顶部导航：Logo、GitHub 链接、主题切换。
2. Hero 区：一句定位文案，例如“我常用的 AI 与开发工具”。
3. 搜索区：大号搜索框，支持实时搜索。
4. 分类栏：胶囊按钮或横向滚动 tab。
5. 卡片区：响应式网格。
6. 页脚：README 驱动说明、仓库地址。

### 卡片字段

| 字段 | 是否必须 | 说明 |
|---|---|---|
| 名称 | 是 | 工具主标题 |
| URL | 是 | 点击跳转目标 |
| 描述 | 是 | 一句话概述 |
| 分类 | 是 | 由 README 标题推导 |
| 标签 | 否 | 第二阶段支持 |
| 推荐标记 | 否 | 第二阶段支持 |

## 视觉风格建议

本项目应采用“editorial + product UI”的混合风格，而不是传统网址导航站模板。视觉参考更接近现代产品站与轻量设计系统，强调留白、克制的色彩和优雅的卡片层级。

建议的视觉关键词：
- 深色优先，但支持浅色主题。
- 低饱和中性色背景。
- 单一强调色用于搜索、按钮和高亮状态。
- 大标题 + 紧凑副标题。
- 卡片统一圆角与边框，少用夸张投影。

## 竞品与替代方案比较

| 方案 | 优点 | 缺点 | 是否适合本项目 |
|---|---|---|---|
| 直接用 README | 维护最简单 | 展示弱、缺少设计感 | 否 |
| 直接 fork van579/nav | 交互成熟 | 后端与管理系统过重 | 否 |
| 直接用 Homepage | 设计成熟 | 偏 dashboard，不符合 README 驱动 | 否 |
| README + 自建静态展示层 | 内容与展示解耦，最灵活 | 需要自行实现解析与 UI | 是 |

## 里程碑规划

### M1：数据链路验证

目标是完成从 README 到 JSON 的稳定转换。验收标准：能够正确解析不少于 3 个分类、每类不少于 3 个工具条目，并输出可供前端消费的 JSON 文件。

### M2：页面 MVP

目标是完成基础静态页面。验收标准：支持首页加载、分类筛选、实时搜索、卡片跳转和深浅主题切换。

### M3：视觉优化

目标是提升设计感和品牌表达。验收标准：完成 Hero 区、卡片微交互、移动端适配和视觉统一性优化。

### M4：工程完善

目标是接入自动构建和静态部署。验收标准：提交 README 后可自动生成页面并发布至 GitHub Pages。

## 验收标准

### 功能验收

- 修改 README 后可以稳定生成结构化数据。
- 页面可正常展示全部分类和工具项。
- 搜索能够基于名称和描述过滤结果。
- 点击卡片可打开对应外部链接。
- 深浅主题切换正常。
- 桌面端和移动端都可正常使用。

### 体验验收

- 首屏 3 秒内能理解页面用途。
- 搜索操作足够直接，不需要学习成本。
- 卡片信息一眼可扫读，不显拥挤。
- 页面有设计感，但不过度装饰。
- 与 GitHub README 的内容保持一致，不出现“README 有，页面没有”或反向差异。

### 工程验收

- 无后端依赖。
- 无数据库依赖。
- 构建后可产出纯静态资源。
- 可部署到 GitHub Pages。

## 风险与应对

### 风险一：README 结构不稳定

如果 README 写法随意，解析逻辑会变脆。应对方式是定义严格、简单的 Markdown 约定，并在构建时做格式校验。

### 风险二：页面设计过于像传统导航站

如果只是把链接渲染成很多卡片，最终会缺少辨识度。应对方式是强化 Hero、标题、留白、品牌文案和卡片排版节奏，减少“模板导航站感”。

### 风险三：过早引入复杂字段

若首期就加入标签、多语言、评分、推荐等级等，README 维护成本会迅速上升。应对方式是首期只保留最小字段集：名称、链接、描述、分类。

### 风险四：直接复用 van579/nav 造成架构污染

如果沿用其工程结构，项目会被后台、数据库和服务配置拖累，偏离纯静态目标。应对方式是只借鉴其前端交互，不复用其系统架构。

## 最终建议

本项目的最佳实施路径不是“改造一个导航系统”，而是“以 README 为中心，重建一个轻量前端展示层”。`Awesome-Tools` 提供内容组织模型，`van579/nav` 提供交互启发，而真正的落地方案应当是纯静态、可构建、以搜索和卡片浏览为核心的自定义页面。

首期推荐以最小结构落地：README 规范、构建脚本、JSON 数据、单页展示站。待内容规模和访问需求增长后，再逐步演进为更完整的设计系统和多字段数据模型。
